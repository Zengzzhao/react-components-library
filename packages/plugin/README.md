# hkx-plugin

Contains small plugin utilities used across hkx projects:

- `request` — lightweight fetch wrapper that returns JSON or text
- `Updater` — in-memory helper to add/update items


